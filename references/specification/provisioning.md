# Tilde Provisioning

This routed specification fragment delegates to the canonical contract.

The canonical stateless provisioning contract lives in [Specification](../specification.md). For provisioning behavior,
read these sections there:

- `2. Core Invariants`
- `3. Persistent State`
- `6. Planning`
- `7. Command Matrix`
- `13. Commit-Diff Cleanup`
- `14. Targeted Live Checks`
